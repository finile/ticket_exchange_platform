<% if ticket.is_favorited?(current_user) %>
  <%= link_to '不收藏', unfavorite_ticket_path(ticket), method: :post, class: "btn btn-info" %>
<% else %>
  <%= link_to '收藏', favorite_ticket_path(ticket), method: :post, class: "btn btn-primary" %>
<% end %>
