<% if parkticket.is_favorited?(current_user) %>
  <%= link_to '不收藏', unfavorite_parkticket_path(parkticket), method: :post, class: "btn btn-info" %>
<% else %>
  <%= link_to '收藏', favorite_parkticket_path(parkticket), method: :post, class: "btn btn-primary" %>
<% end %>
